-- a script thay lists alll records of the table second_table of the
--database hbtn_oc_o
-- results displays both score and the name
-- records should be ordered by score.

SELECT score, name FROM second_table ORDER BY score DESC;
